# Migration `20200419133932-init`

This migration has been generated by Tom Chauveau at 4/19/2020, 1:39:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "schema"."Project" ALTER COLUMN "airtable" DROP NOT NULL,
ALTER COLUMN "github" DROP NOT NULL;

ALTER TABLE "schema"."User" ALTER COLUMN "token" DROP NOT NULL;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200419130654-init..20200419133932-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -21,10 +21,10 @@
 model Project {
     id       Int     @default(uuid()) @id
     name     String
     status   Status
-    airtable String
-    github   String
+    airtable String?
+    github   String?
     user     User    @relation(fields: [userId], references: [id])
     userId   Int
 }
@@ -44,8 +44,8 @@
 // User model
 model User {
     id       Int     @default(uuid()) @id
     mail     String  @unique
-    token    String  @unique
-    roles    Roles
+    token    String?  @unique
+    roles    Roles?
     project  Project[]
 }
```


