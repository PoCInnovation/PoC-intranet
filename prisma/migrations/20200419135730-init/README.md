# Migration `20200419135730-init`

This migration has been generated by Tom Chauveau at 4/19/2020, 1:57:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200419133932-init..20200419135730-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -42,10 +42,10 @@
 }
 // User model
 model User {
-    id       Int     @default(uuid()) @id
-    mail     String  @unique
+    id       Int      @default(autoincrement()) @id
+    mail     String   @unique
     token    String?  @unique
     roles    Roles?
     project  Project[]
 }
```


