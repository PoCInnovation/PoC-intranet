# Migration `20200419141828-init`

This migration has been generated by Tom Chauveau at 4/19/2020, 2:18:29 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "public"."Project","public"."Roles","public"."User";

CREATE TYPE "Status" AS ENUM ('Todo', 'InProgress', 'Done');

CREATE TABLE "public"."Project" (
    "airtable" text   ,
    "github" text   ,
    "id" text  NOT NULL ,
    "name" text  NOT NULL ,
    "status" "Status" NOT NULL ,
    "userId" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Roles" (
    "add_member" boolean  NOT NULL DEFAULT false,
    "add_role" boolean  NOT NULL DEFAULT false,
    "admin" boolean  NOT NULL DEFAULT false,
    "create_pro" boolean  NOT NULL DEFAULT false,
    "id" text  NOT NULL ,
    "name" text  NOT NULL ,
    "recommend" boolean  NOT NULL DEFAULT false,
    "userId" text  NOT NULL ,
    "write" boolean  NOT NULL DEFAULT false,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."User" (
    "id" text  NOT NULL ,
    "mail" text  NOT NULL ,
    "token" text   ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "Roles.name" ON "public"."Roles"("name")

CREATE UNIQUE INDEX "Roles_userId" ON "public"."Roles"("userId")

CREATE UNIQUE INDEX "User.mail" ON "public"."User"("mail")

CREATE UNIQUE INDEX "User.token" ON "public"."User"("token")

ALTER TABLE "public"."Project" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Roles" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200419135730-init..20200419141828-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -18,33 +18,33 @@
     Done
 }
 model Project {
-    id       Int     @default(uuid()) @id
+    id       String     @default(uuid()) @id
     name     String
     status   Status
     airtable String?
     github   String?
     user     User    @relation(fields: [userId], references: [id])
-    userId   Int
+    userId   String
 }
 model Roles {
-    id          Int     @default(uuid()) @id
+    id          String     @default(uuid()) @id
     name        String  @unique
     admin       Boolean @default(false)
     add_role    Boolean @default(false)
     recommend   Boolean @default(false)
     write       Boolean @default(false)
     add_member  Boolean @default(false)
     create_pro  Boolean @default(false)
     user        User    @relation(fields: [userId], references: [id])
-    userId      Int
+    userId      String
 }
 // User model
 model User {
-    id       Int      @default(autoincrement()) @id
+    id       String      @default(uuid()) @id
     mail     String   @unique
     token    String?  @unique
     roles    Roles?
     project  Project[]
```


