# Migration `20200618165035-update`

This migration has been generated by Vasek - Tom C. at 6/18/2020, 4:50:36 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Project" ADD COLUMN "description" text   ;

ALTER TABLE "public"."Role" ADD COLUMN "color" text  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200426155558-init..20200618165035-update
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -20,8 +20,9 @@
 model Project {
     id          String    @default(uuid()) @id
     name        String    @unique
+    description String?
     status      Status
     airtable    String?
     github      String?
     users       User[]    @relation(references: [id])
@@ -30,8 +31,9 @@
 // Roles
 model Role {
     id          String   @default(uuid()) @id
     name        String   @unique
+    color       String
     admin       Boolean? @default(false)
     add_role    Boolean? @default(false)
     recommend   Boolean? @default(false)
     write       Boolean? @default(false)
```


