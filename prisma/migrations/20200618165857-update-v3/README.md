# Migration `20200618165857-update-v3`

This migration has been generated by Vasek - Tom C. at 6/18/2020, 4:58:57 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Role" ADD COLUMN "color" text   DEFAULT 'white';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200618165814-update-correction..20200618165857-update-v3
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -31,9 +31,9 @@
 // Roles
 model Role {
     id          String   @default(uuid()) @id
     name        String   @unique
-    color       String?
+    color       String?  @default("white")
     admin       Boolean? @default(false)
     add_role    Boolean? @default(false)
     recommend   Boolean? @default(false)
     write       Boolean? @default(false)
```


